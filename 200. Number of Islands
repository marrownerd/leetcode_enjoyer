class Solution(object):
    def numIslands(self, grid):
        if not grid: return 0
        rows, cols = len(grid), len(grid[0])
        islands = 0
        
        for r in range(rows):
            for c in range(cols):
                if grid[r][c] == '1':
                    islands += 1
                    self.sink_island(grid, r, c)
        return islands

    def sink_island(self, grid, r, c):
        rows, cols = len(grid), len(grid[0])
        
        if r < 0 or r >= rows or c < 0 or c >= cols or grid[r][c] == '0':
            return
        
        grid[r][c] = '0'
        

        self.sink_island(grid, r + 1, c) 
        self.sink_island(grid, r - 1, c) 
        self.sink_island(grid, r, c + 1) 
        self.sink_island(grid, r, c - 1)
        
